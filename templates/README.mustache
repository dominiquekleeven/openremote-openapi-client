# {{{projectName}}}

`openremote-openapi-client` is a mostly auto-generated Python client library for the OpenRemote Manager REST API.  
It includes an `OpenRemoteApiClient` that automatically handles the authentication with Keycloak, and the OAuth2 token lifecycle.  

Refer to the official [OpenRemote documentation](https://docs.openremote.io/) for details on the platform.  

## Features

- **Auto-generated** from the OpenAPI specification using OpenAPI Generator with some additional features
- **Automated authentication** with Keycloak and token lifecycle management
- **Full coverage** of OpenRemote Manager API endpoints

This package is generated by the [OpenAPI Generator](https://openapi-generator.tech):  

- API version: {{appVersion}}  
- Package version: {{packageVersion}}  
{{^hideGenerationTimestamp}}
- Build date: {{generatedDate}}  
{{/hideGenerationTimestamp}}
- Generator version: {{generatorVersion}}  
- Generator class: {{generatorClass}}  
{{#infoUrl}}
For more information, see [{{{infoUrl}}}]({{{infoUrl}}}).  
{{/infoUrl}}

Note: Some additional changes have been made to the templates of the auto-generated code, such as including the custom `OpenRemoteApiClient`

## Installation

```bash
pip install openremote-openapi-client
```

Or, if you’re using [uv](https://github.com/astral-sh/uv):

```bash
uv add openremote-openapi-client
```

## Quick Start

```python
from {{{packageName}}} import OpenRemoteApiClient, StatusApi, AssetApi, AssetQuery

# Create the OpenRemote Client
client = OpenRemoteApiClient(
    base_url="http://localhost:8080/api/master",
    keycloak_url="http://localhost:8081/auth",
    client_id="serviceuser",
    client_secret="your_secret",
    realm="master"
)

# Health check
status_api = StatusApi(client)
health = status_api.get_health_status()
print(f"System health: {health}")

# Query assets
asset_api = AssetApi(client)
assets = asset_api.query_assets(asset_query=AssetQuery(limit=10))
print(f"Found {len(assets)} assets")
```

{{> common_README }}
